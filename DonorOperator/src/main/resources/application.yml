spring:
  datasource:
    url: 'jdbc:mysql://localhost:3306/DonorOperator'
    username: Ameena
    password: Ameena
  application:
    name: DonorOperator
server:
  port: 8081
      
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone : http://localhost:8761/eureka/